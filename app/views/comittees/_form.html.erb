<%= form_for(@comittee) do |f| %>
  <% if @comittee.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comittee.errors.count, "error") %> prohibited this comittee from being saved:</h2>

      <ul>
      <% @comittee.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
  <%= f.collection_select :voivodeship_ids, Voivodeship.all, :id, :name, {}, {:multiple => true} %>
  </div>
  <div class="field">
    <%= f.file_field :avatar %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
